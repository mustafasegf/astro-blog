---
// import translations
import { useLang } from "@utils/t.ts";
import Layout from "@layouts/Layout.astro";
import Container from "@src/components/Container.astro";

const t = useLang(Astro.url);
---

<Layout>
	<Container class="pt-16">
		<span set:html={t("home.description")} />
	</Container>
</Layout>
